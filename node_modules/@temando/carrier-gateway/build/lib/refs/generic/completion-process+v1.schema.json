{
  "id": "http://carrier-gateway.temando.com/schema/generic/completion-process+v1#",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "description": "A schema to describe the steps taken upon completion with a specific carrier. closeoutAccount indicates all booking related to the account will be affected. closeoutOrigin indicates all bookings realted to the origin will be affected. closeoutServiceGroup indicates all booking related to that service group will be affected.",
  "type": "array",
  "minLength": 1,
  "items": {
    "type": "string",
    "enum": [
      "transmit",
      "pickup",
      "closeoutAccount",
      "closeoutOrigin",
      "closeoutServiceGroup"
    ]
  }
}
