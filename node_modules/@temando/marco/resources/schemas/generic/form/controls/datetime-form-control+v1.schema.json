{
  "id": "https://app.temando.com/schemas/generic/form/controls/datetime-form-control+v1#",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "description": "A control for a date value (string, ISO 8601 date format). The user should be able to set the date and the time.",
  "required": ["type"],
  "properties": {
    "type": {
      "type": "string",
      "enum": ["datetime"]
    }
  }
}
