const chai = require('chai');
chai.use(require('dirty-chai'));
const expect = chai.expect;

const marco = require('../../index');

const sampleForm = require('../../resources/samples/sample-marco-form-response.json');

describe('Testing sample form with all options in', function() {
  it('should be valid against schema', function() {
    const isValid = marco.v1.validate(
      'https://app.temando.com/schema/generic/marco-definition+v1#',
      sampleForm
    );

    if (!isValid) console.log(marco.v1.errors);

    expect(isValid).to.be.ok();
  });
});
