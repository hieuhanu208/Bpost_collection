import { ISovereigntyMap, ITemandoEnvironment, ITemandoServiceDefinition } from './types';
/**
 * Get & decorate a ts-definition
 */
export declare function getServiceDefinition(input: string | object): ITemandoServiceDefinition;
/**
 * Get & decorate a ts-env
 */
export declare function getServiceEnv(input: string | object): ITemandoEnvironment;
/**
 * Maps known sovereigns to allowed AWS regions.
 *
 * This will eventually be driven by config, or from some other data store.
 */
export declare const SovereigntyMap: ISovereigntyMap;
