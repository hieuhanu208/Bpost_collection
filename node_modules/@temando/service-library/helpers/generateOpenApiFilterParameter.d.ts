import { IFilterField, IFilterFieldsConfig } from '../middlewares/SearchFilter';
export interface IOpenApiV2QueryParameter {
    name: string;
    in: 'query';
    required: boolean;
    description: string;
    type: string;
}
/**
 * Generate query parameter for Open API v2
 *
 * Describe filter based on field fields configuration.
 * If no example is given, an example will be generated using the
 * first filter field configuration.
 */
export declare const generateOpenApiFilterParameter: (filterFieldsConfig: IFilterFieldsConfig, example?: IFilterField[]) => IOpenApiV2QueryParameter;
