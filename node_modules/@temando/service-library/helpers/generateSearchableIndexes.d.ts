import { IFilterFieldsConfig } from '../middlewares/SearchFilter';
export interface IGenerateSearchableIndexesConfig {
    document: {
        [key: string]: any;
    };
    filterFieldsConfig: IFilterFieldsConfig;
}
/**
 * Given a document object and a valid filter fields config, generate a list of
 * key value pairs that can be passed to storage as part of the `Attributes`
 * list to be indexed.
 */
export declare function generateSearchableIndexes(config: IGenerateSearchableIndexesConfig): {
    [key: string]: any;
};
