import { StorageLibrary } from '@temando/storage-library';
import { Mediator } from '../framework/Mediator';
import { IAddServiceClientFactoryEvent } from './AddServiceClientFactory';
export interface IAddStorageEvent extends IAddServiceClientFactoryEvent {
    storage: StorageLibrary;
}
/**
 * This middleware attaches an instance of Storage Library to the event.
 * Note that tsDefinition and tsEnv are only required if the Instrumentation
 * middleware is not being used, as Instrumentation middleware already adds
 * serviceClientFactory to the event.
 */
export declare function AddStorageLibrary(): (m: Mediator) => void;
