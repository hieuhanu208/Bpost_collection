import { InstrumentClient } from '@temando/instrument';
import { ITemandoServiceDefinition } from '@temando/service-client-factory/types';
import { Mediator } from '../framework/Mediator';
import { ILambdaHttpEvent } from '../types';
export interface IInstrumentationEvent extends ILambdaHttpEvent {
    instrumentation: InstrumentClient;
}
export interface IInstrumentationParams {
    /** A name, for logging purposes - typically the function name */
    name?: string;
    tsDefinition: ITemandoServiceDefinition;
}
/**
 * Instrumentation middleware
 *
 * Integrates with:
 * - `Http` middleware
 * - `AddServiceClientFactory` middleware
 * - Any handler
 */
export declare function Instrumentation({tsDefinition}: IInstrumentationParams): (m: Mediator) => void;
