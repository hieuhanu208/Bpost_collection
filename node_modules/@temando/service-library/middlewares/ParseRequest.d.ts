import { ITemandoServiceDefinition } from '@temando/service-client-factory/types';
import { Mediator } from '../framework/Mediator';
import { ILambdaHttpEvent } from '../types';
export interface IParseRequestEvent extends ILambdaHttpEvent {
    accountId: string;
    sovereignty: string;
    origin: string;
    code: string;
}
/**
 * This middleware prepares the `event` with extra properties:
 *
 * - `event.accountId`
 * - `event.sovereignty`
 * - `event.origin`
 * - `event.code`
 *
 * Integrates with:
 * - `Http` middleware
 */
export declare function ParseRequest({tsDefinition}: {
    tsDefinition?: ITemandoServiceDefinition;
}): (m: Mediator) => void;
