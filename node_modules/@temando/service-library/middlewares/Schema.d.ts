import { Mediator } from '../framework/Mediator';
import { IValidate } from '../schema/SchemaValidator';
export interface ISchemaEventPartial {
    validate: IValidate;
}
/**
 * Supplies `event.validate`, a validator function from `SchemaValidator`
 */
export declare function Schema(schema: any): (m: Mediator) => void;
