"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const SchemaValidator_1 = require("../schema/SchemaValidator");
/**
 * Supplies `event.validate`, a validator function from `SchemaValidator`
 */
function Schema(schema) {
    if (!schema) {
        throw new Error('[Middleware][Schema] Missing `schema`');
    }
    const validate = SchemaValidator_1.SchemaValidator(schema);
    return (m) => {
        console.warn('[DEPRECATION] Schema middleware has been deprecated, use the SchemaValidator function on its own');
        m.on('event', (event) => {
            event.validate = validate;
        });
    };
}
exports.Schema = Schema;
//# sourceMappingURL=Schema.js.map