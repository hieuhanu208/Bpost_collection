import * as Joi from 'joi';
import { ITimer, ITimingArgs } from '../types';
/**
 * Parses arguments between two patterns:
 *   - arg1, arg2, arg3
 *   - { arg1, arg2, arg3 }
 */
export declare function parseArguments(args: any[], keys: string[]): any;
/**
 * Joi validates a given payload
 */
export declare function validate(payload: any, schema: Joi.Schema): Joi.ValidationResult<any>;
/**
 * hrtime to microseconds
 *
 * It will also always round up to nearest μs.
 *
 * Not milliseconds (thousandth), but microseconds (millionth).
 *
 * https://en.wikipedia.org/wiki/Orders_of_magnitude_(time)
 */
export declare function hrTimeToMicroseconds(hrtime: [number, number]): number;
/**
 * Check if it is a HR timer sort of.
 *
 * @param timing
 * @returns {boolean}
 */
export declare function isTimer(timing: ITimingArgs['value'] | ITimer): timing is ITimer;
