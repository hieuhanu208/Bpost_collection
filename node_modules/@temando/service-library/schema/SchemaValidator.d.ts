export declare type ISchemaValidator = (payload: {
    [key: string]: any;
}) => boolean | never;
export interface IValidate {
    (key: string, payloadArg: {
        [key: string]: any;
    }): boolean | never;
    (key: string): ISchemaValidator;
}
/**
 * @constructor
 *
 * Create a validator function to wrap AjvSchema and output a standard error.
 * Expects `ajvSchema.addSchema()` pattern.
 *
 * @example
 *
 * const validator = SchemaValidator(ajvSchema);
 * validator('some-schema-key', payload); // May throw ValidationError
 * validator('some-schema-key', payload); // May throw ValidationError
 *
 */
export declare function SchemaValidator(schema: any): IValidate;
