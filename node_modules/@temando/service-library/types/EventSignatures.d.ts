import { IListenerConfig } from '../framework/Event';
import { ILambdaBatchEventSignatures, ILambdaEventSignatures, ILambdaHttpEventSignatures } from '../handlers/index';
import { IHttpMwEventSignatures } from '../middlewares/Http';
import { IKinesisMwEventSignatures } from '../middlewares/Kinesis';
export interface IBaseEventSignature {
    (key: string, cb: (event: any) => any, options?: IListenerConfig): any;
}
/**
 * Event .on() call signatures are consolidated in one place to provide intellisense
 * and a centralized event documentation source
 */
export declare type IEventSignatures = (IHttpMwEventSignatures<IListenerConfig> & ILambdaBatchEventSignatures<IListenerConfig> & ILambdaHttpEventSignatures<IListenerConfig> & ILambdaEventSignatures<IListenerConfig> & IKinesisMwEventSignatures<IListenerConfig> & IBaseEventSignature);
