'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = constructEqualCondition;
/**
 * Constructs a MongoDB style equal condition.
 *
 * @param {string} key
 * @param {string} value
 *
 * @returns {Object}
 */
function constructEqualCondition(key, value) {
  const condition = {};
  let conditionValue = value;

  // Cast boolean strings appropriately as they will have been
  // cast when the definition was saved
  if (value === 'true' || value === 'false') {
    conditionValue = value === 'true';
  }

  condition[`definition.attributes.${key}`] = {
    $eq: conditionValue
  };

  return condition;
}
module.exports = exports['default'];