#!/usr/bin/env node
'use strict';

var _commander = require('commander');

var _commander2 = _interopRequireDefault(_commander);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

_commander2.default.version(require('../package.json').version).command('kv [command]', 'access to the key/value store').command('catalog [command]', 'access to the service catalog').command('service [command]', 'create and resolve services from the catalog').command('watch', 'watches a local registry for changes').parse(process.argv);

if (!process.argv.slice(2).length) {
  _commander2.default.outputHelp();
}