'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _bluebird = require('bluebird');

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _jsonfile = require('jsonfile');

var _jsonfile2 = _interopRequireDefault(_jsonfile);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

var LocalFileSystem = function () {
  function LocalFileSystem(dir) {
    _classCallCheck(this, LocalFileSystem);

    this.dir = dir;

    // @todo check if dir exists, if not, try and make it.
  }

  _createClass(LocalFileSystem, [{
    key: 'read',
    value: function () {
      var _ref = (0, _bluebird.coroutine)(function* (key) {
        try {
          var obj = _jsonfile2.default.readFileSync(`${this.dir}/${key}`);

          return obj;
        } catch (err) {
          throw err;
        }
      });

      function read(_x) {
        return _ref.apply(this, arguments);
      }

      return read;
    }()
  }, {
    key: 'write',
    value: function () {
      var _ref2 = (0, _bluebird.coroutine)(function* (key, data) {
        // @todo key can include directories that may not exist yet.
        try {
          _jsonfile2.default.writeFileSync(`${this.dir}/${key}`, data);

          return true;
        } catch (err) {
          throw err;
        }
      });

      function write(_x2, _x3) {
        return _ref2.apply(this, arguments);
      }

      return write;
    }()
  }, {
    key: 'list',
    value: function () {
      var _ref3 = (0, _bluebird.coroutine)(function* (prefix) {
        // @todo - remove me when done
        void prefix; // eslint-disable-line
        return ['local', 'fs'];
      });

      function list(_x4) {
        return _ref3.apply(this, arguments);
      }

      return list;
    }()
  }]);

  return LocalFileSystem;
}();

exports.default = LocalFileSystem;
module.exports = exports['default'];