/// <reference types="archiver" />
import { Archiver } from 'archiver';
import { Logger } from './lib/Logger';
import { WebpackBuilder } from './WebpackBuilder';
export declare class FileBuild {
    logger: Logger;
    externals: Set<string>;
    webpackBuilder: WebpackBuilder;
    servicePath: string;
    buildTmpDir: string;
    tryFiles: string[];
    handlerEntryExt: string;
    constructor(config: {
        logger: Logger;
        servicePath: string;
        buildTmpDir: string;
        tryFiles: string[];
        handlerEntryExt: string;
    });
    /**
     *  Handles building from a build file's output.
     */
    build(fnConfig: {
        handler: string;
    }, archive: Archiver): Promise<this>;
    /**
     *  Allows for build files to be auto selected
     */
    private tryBuildFiles();
}
